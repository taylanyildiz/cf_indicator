name: cf_indicator
description: This package has a indicator with pageView on page.it's responsive and can chage postion of indicator.
version: 0.0.7
homepage: https://github.com/taylanyildiz/cf_indicator.git

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  assets:
     - assets/images/
